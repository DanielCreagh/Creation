language: objective-c

install:
  - bundle install

before_script:
  - bundle exec rake:test_command

script: xcodebuild -project Creation.xcodeproj -target Creation -sdk iphonesimulator ONLY_ACTIVE_ARCH=NO